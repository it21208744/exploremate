import styled from 'styled-components'

const Wrapper = styled.div`
  .pageTitle {
    position: relative;
    top: -16vw;
    left: 25vw;
  }

  .form {
    position: relative;
    top: -14vw;
    /* left: 25vw; */
  }

  .defaultContent {
    position: absolute;
    /* left: 25vw; */
    width: 100%;
    height: 100vw;
    background: wheat;
  }
`

export default Wrapper
