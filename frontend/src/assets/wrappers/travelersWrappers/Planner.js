import styled from 'styled-components'

const Wrapper = styled.div`
  .planner {
    position: relative;
    left: 300px; /* Adjust the distance based on the width of .absolute-div */
  }
`

export default Wrapper
